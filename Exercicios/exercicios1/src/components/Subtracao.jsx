import React from 'react'

const Subtracao = (props) => {

    return (

        <div>O resultado da <b>subtração</b> num1 - num 2 é: {props.num1-props.num2}</div>

    );

};

export default Subtracao;
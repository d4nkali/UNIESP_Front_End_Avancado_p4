import React from 'react'

const Subtracao = (props) => {

    return (

        <div>O resultado da <b>subtração</b> num1 (32) - num 2 (9) é: {props.num1-props.num2}</div>

    );

};

export default Subtracao;